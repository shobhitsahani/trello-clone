{
  "name": "task-checker-root",
  "private": true,
  "packageManager": "pnpm@10.34.3",
  "scripts": {
    "predev": "pnpm install",
    "dev": "pnpm --dir taskChecker/apps/frontend run dev",
    "prebuild": "pnpm install",
    "build": "pnpm --dir taskChecker/apps/frontend run build",
    "lint": "pnpm --dir taskChecker run lint",
    "format": "pnpm --dir taskChecker run format",
    "check-types": "pnpm --dir taskChecker run check-types"
  },
  "devDependencies": {
    "turbo": "^2.11.2"
  }
}
